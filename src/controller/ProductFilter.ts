import JSZip from "jszip";
import * as fs from "fs-extra";

export default class ProductFilter {
    constructor() {

    }

    public async Loooolaite(): Promise<string> {
        const zip = new JSZip();
        return Promise.resolve("yes");
    }
}